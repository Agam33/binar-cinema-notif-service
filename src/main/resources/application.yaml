app:
  firebase:
    file: binar-ra-cinema-firebase.json

server:
  port: 8081

spring:
  application:
    name: NOTIFICATION-SERVICE

eureka:
  instance:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka